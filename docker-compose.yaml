version: "3.9"

services:
  app:
    build:
      context: .
    ports:
      - "8777:8777"
    volumes:
      - .:/app
    # command: >
    #   sh -c "python manage.py makemigrations &&
    #          python manage.py migrate &&
    #          python manage.py runserver 0.0.0.0:8000"
    env_file:
      - .env
    networks:
      - postgres15-network
networks:
  postgres15-network:
    external: true
